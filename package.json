{
  "name": "markets-lab-dashboard",
  "version": "1.1.2",
  "private": true,
  "description": "Markets Lab dashboard publisher — lab/personal ledgers and fill normalization",
  "type": "module",
  "scripts": {
    "test": "node --test tests/**/*.test.js && python3 -m unittest discover -s tests -p 'test_*.py' -v"
  },
  "engines": {
    "node": ">=18"
  }
}
